Update a Jira issue.

Usage: /update PROJ-123 --status "Done"

Execute: `jira-tool update $ARGUMENTS`

Common options:
- `--status "Done"` - Transition to new status
- `--summary "New title"` - Update summary
- `--assignee "username"` - Change assignee
- `--priority High` - Change priority
